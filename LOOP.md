# LOOP.md

- Iteration 1: Maker created the Loop Lab static app shell; checker target is TestSprite CLI against the public GitHub Pages URL; expected first failures are around empty-state rendering, form validation, filter behavior, and persistence; maker will fix issues found by the checker.
